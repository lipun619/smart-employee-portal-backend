# ============================================================
# Backend CI/CD Pipeline — Smart Employee Portal .NET 10 API
# ============================================================
# Triggers on every push to main branch (src folder changes only)
# Steps: restore → build → test → publish → deploy to Azure App Service

trigger:
  branches:
    include:
      - master
      - main
  paths:
    include:
      - src/**
      - tests/**
      - azure-pipelines.yml

pool:
  vmImage: 'windows-latest'

variables:
  buildConfiguration: 'Release'
  dotnetVersion: '10.x'
  apiProjectPath: 'src/SmartEmployeePortal.API/SmartEmployeePortal.API.csproj'
  functionProjectPath: 'src/SmartEmployeePortal.Functions/SmartEmployeePortal.Functions.csproj'
  testProjectPath: 'tests/SmartEmployeePortal.Application.Tests/SmartEmployeePortal.Application.Tests.csproj'
  apiAppName: 'api-sep-dev'
  functionAppName: 'func-sep-dev'

stages:

  # ── Stage 1: Build & Test ────────────────────────────────────────────────
  - stage: Build
    displayName: 'Build & Test'
    jobs:
      - job: BuildAndTest
        displayName: 'Build and Run Tests'
        steps:

          - task: UseDotNet@2
            displayName: 'Install .NET $(dotnetVersion)'
            inputs:
              version: $(dotnetVersion)
              includePreviewVersions: true

          - task: DotNetCoreCLI@2
            displayName: 'Restore API project'
            inputs:
              command: 'restore'
              projects: $(apiProjectPath)

          - task: DotNetCoreCLI@2
            displayName: 'Restore Function project'
            inputs:
              command: 'restore'
              projects: $(functionProjectPath)

          - task: DotNetCoreCLI@2
            displayName: 'Build API project'
            inputs:
              command: 'build'
              projects: $(apiProjectPath)
              arguments: '--configuration $(buildConfiguration) --no-restore'

          - task: DotNetCoreCLI@2
            displayName: 'Build Function project'
            inputs:
              command: 'build'
              projects: $(functionProjectPath)
              arguments: '--configuration $(buildConfiguration) --no-restore'

          - task: DotNetCoreCLI@2
            displayName: 'Run unit tests'
            inputs:
              command: 'test'
              projects: $(testProjectPath)
              arguments: '--configuration $(buildConfiguration) --no-build --collect:"XPlat Code Coverage"'
              publishTestResults: true

          - task: DotNetCoreCLI@2
            displayName: 'Publish API'
            inputs:
              command: 'publish'
              projects: $(apiProjectPath)
              arguments: '--configuration $(buildConfiguration) --output $(Build.ArtifactStagingDirectory)/api'

          - task: DotNetCoreCLI@2
            displayName: 'Publish Azure Function App'
            inputs:
              command: 'publish'
              projects: $(functionProjectPath)
              arguments: '--configuration $(buildConfiguration) --output $(Build.ArtifactStagingDirectory)/functions'

          - task: PublishBuildArtifacts@1
            displayName: 'Upload API artifact'
            inputs:
              pathToPublish: '$(Build.ArtifactStagingDirectory)/api'
              artifactName: 'SmartEmployeePortal-API'

          - task: PublishBuildArtifacts@1
            displayName: 'Upload Function artifact'
            inputs:
              pathToPublish: '$(Build.ArtifactStagingDirectory)/functions'
              artifactName: 'SmartEmployeePortal-Functions'

  # ── Stage 2: Deploy to Azure App Service ────────────────────────────────
  - stage: Deploy
    displayName: 'Deploy to Azure'
    dependsOn: Build
    condition: and(succeeded(), or(eq(variables['Build.SourceBranch'], 'refs/heads/main'), eq(variables['Build.SourceBranch'], 'refs/heads/master')))
    jobs:
      - deployment: DeployAPI
        displayName: 'Deploy .NET API and Azure Functions to Azure'
        environment: 'Development'
        strategy:
          runOnce:
            deploy:
              steps:
                - task: AzureWebApp@1
                  displayName: 'Deploy API to App Service'
                  inputs:
                    azureSubscription: 'AZURE_SERVICE_CONNECTION'
                    appType: 'webApp'
                    appName: '$(apiAppName)'
                    package: '$(Pipeline.Workspace)/SmartEmployeePortal-API/**/*.zip'
                    deploymentMethod: 'auto'

                - task: AzureFunctionApp@1
                  displayName: 'Deploy Azure Function App'
                  inputs:
                    azureSubscription: 'AZURE_SERVICE_CONNECTION'
                    appType: 'functionApp'
                    appName: '$(functionAppName)'
                    package: '$(Pipeline.Workspace)/SmartEmployeePortal-Functions/**/*.zip'
                    deploymentMethod: 'auto'
